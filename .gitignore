# Ephemeral pipeline scratch artifacts (regenerated by tools; NOT durable knowledge).
# Root-level only: session crystallized reports (<session>/<slug>/report.*.md)
# stay tracked per tacticaldoll.github.io GUIDE §52.
*-report.md
*-evidence.*
terminology.corrections.json

# Per-session workflow trackers (init-handoff / publish-article task.md) are transient.
*/task.md
